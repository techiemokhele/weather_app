import { LocationSearchComponentProps } from "@/types"

const CurrentLocationTempInfoComponent = ({dark}: LocationSearchComponentProps) => {
  return (
    <div>CurrentLocationTempInfoComponent</div>
  )
}

export default CurrentLocationTempInfoComponent